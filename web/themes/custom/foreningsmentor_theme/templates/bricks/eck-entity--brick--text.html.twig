<div{{ attributes.addClass('row','text-area','bg-white') }}>
  <div class="col-12 pt-5 pb-5">
    <div class="container">
      <div class="row">
        <div class="col">
          <h2 class="mb-3">{{ content.title[0]  }}</h2>
          <p class="lead">{{ content.field_lead[0] }}</p>
          {{ content|without('title','field_lead') }}
        </div>
      </div>
    </div>
  </div>
</div>
