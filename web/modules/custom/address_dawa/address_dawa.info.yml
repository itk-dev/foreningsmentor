name: "Address DAWA (deprecated)"
description: "Deprecated. Replaced by the `adressevaelger` module, which provides the same `address_dawa` field type / widget / formatter plugin ids backed by SDFI Adressevælger. This stub remains on disk only so existing installations can run `drush pmu address_dawa` after importing the new config."
type: module
core_version_requirement: ^10 || ^11
package: "Foreningsmentor"
hidden: true
