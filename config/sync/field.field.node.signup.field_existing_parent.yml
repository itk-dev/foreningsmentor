uuid: c270b78c-0f16-47a9-a8bb-d2288857ae6d
langcode: da
status: true
dependencies:
  config:
    - field.storage.node.field_existing_parent
    - node.type.parent
    - node.type.signup
id: node.signup.field_existing_parent
field_name: field_existing_parent
entity_type: node
bundle: signup
label: 'Eksisterende forælder i systemet'
description: 'Hvis systemet rapporterer at der er fundet en lignende forælder i systemet og du kan bekræfte at det er den samme forælder som angivet herover, bør du pege på denne i dette felt. Herved knyttes barnet til den eksisterende forælder i systemet i stedet for at oprette en ny.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:node'
  handler_settings:
    target_bundles:
      parent: parent
    sort:
      field: _none
      direction: ASC
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
